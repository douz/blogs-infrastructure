apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: certificate-config
  namespace: argocd
  labels:
    app.kubernetes.io/name: certificate-config
    app.kubernetes.io/part-of: argocd
spec:
  project: platform
  source:
    repoURL: https://github.com/douz/blogs-infrastructure.git
    targetRevision: main
    path: clusters/prod/platform/certificate-config
  destination:
    server: https://kubernetes.default.svc
    namespace: cert-manager
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - FailOnSharedResource=true
